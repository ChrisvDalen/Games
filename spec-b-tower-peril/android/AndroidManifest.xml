<?xml version="1.0" encoding="utf-8"?>
<!--
  minSdkVersion/targetSdkVersion mirror the reactor's ${android-min-sdk.version}
  (24) and ${android-sdk.version} (34) properties from the root pom.xml. The
  android-maven-plugin's <sdk><platform> in pom.xml drives the actual compile/
  target SDK used to build; these values are kept in sync by hand since a raw
  AndroidManifest.xml isn't Maven-property-filtered.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moneyfirst.towerperil.android"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        <!--
          Google's published TEST AdMob application id. REPLACE with the real
          app id from the AdMob console before a store release - see
          README.md "Before store submission".
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name="com.moneyfirst.towerperil.android.AndroidLauncher"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
