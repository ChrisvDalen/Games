package com.moneyfirst.wardrobesort;

/** What the player gets in exchange for watching a rewarded video. */
public enum RewardedPurpose {
    /** RoundTimer's one-time +10s extension. */
    EXTRA_TIME,
    /** HintSystem's tray-position reveal. */
    OUTFIT_HINT
}
