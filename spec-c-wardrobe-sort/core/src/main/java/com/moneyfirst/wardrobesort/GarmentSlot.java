package com.moneyfirst.wardrobesort;

/**
 * The five avatar equipment slots a Wardrobe Sort outfit is built from. Order
 * here is the canonical display/iteration order used throughout the engine
 * (top-to-bottom on the avatar silhouette).
 */
public enum GarmentSlot {
    HEAD,
    TOP,
    BOTTOM,
    SHOES,
    ACCESSORY
}
