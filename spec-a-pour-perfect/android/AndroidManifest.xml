<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.moneyfirst.pourperfect.android"
          android:versionCode="1"
          android:versionName="1.0.0">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />

    <!-- Ads (AdMob) and Play Billing both need network access to fetch/verify. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required by the Play Console for apps using Google Play Billing. -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:hardwareAccelerated="true">

        <!--
          AdMob application ID meta-data - REQUIRED by the Google Mobile Ads SDK or the app will
          crash on startup. This is Google's published TEST app ID
          (ca-app-pub-3940256099942544~3347511713) - see docs/MONEY_FIRST_ARCHITECTURE.md and
          README.md's before-submission checklist. Replace with the real AdMob app ID from
          https://apps.admob.com before any store release build.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name="com.moneyfirst.pourperfect.android.AndroidLauncher"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
